# Dónde vive la API.
#
# SIN prefijo NEXT_PUBLIC, y es deliberado: una variable con ese prefijo se
# incrusta en el paquete que se descarga el navegador, y con ella el origen del
# backend queda publicado. Al no llevarlo, sólo existe en el servidor.
API_URL=http://localhost:3000
